$kind: http-request
url: "{{base_url}}/api/donations/history?per_page=10&sort_by=created_at&sort_dir=desc"
method: GET
headers:
  Accept: application/json
  Authorization: Bearer {{token}}
queryParams:
  per_page: "10"
  sort_by: created_at
  sort_dir: desc
order: 1000
