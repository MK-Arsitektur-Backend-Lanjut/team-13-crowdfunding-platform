config:
  target: "http://localhost:8000"
  phases:
    - duration: 30
      arrivalRate: 1
      name: Smoke test (1 VU)
  http:
    timeout: 30
scenarios:
  - name: Smoke test
    flow:
      - get:
          url: "/api/campaigns?page=1"